apiVersion: v1
kind: Service
metadata:
  name: {{ include "restaurant-api.name" . }}
  labels:
    {{- include "restaurant-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "restaurant-api.selectorLabels" . | nindent 4 }}
